// Phase 12.1 — client-side image preprocessing (crop / de-skew / contrast).
// TODO(phase-12): implement with OpenCV.js in a Web Worker.
export async function preprocess(blob: Blob): Promise<Blob> { return blob; }
