// Phase 9.2 — property-based / fuzz tests for the law engine.
// TODO(phase-9): add fast-check properties:
//   - no negative fines
//   - range min <= max
//   - overrides always win over central when valid
//   - deterministic per (inputs, packVersion)
describe.skip('law engine invariants', () => {
  it('TODO', () => {});
});
